#!/bin/bash

git log --all --pretty=format:'%h %s' | perl -CS -ne 'our %seen; my $re=qr/[\x{1F1E6}-\x{1F1FF}\x{2600}-\x{27BF}\x{1F300}-\x{1F6FF}\x{1F900}-\x{1FAFF}\x{20E3}\x{FE0F}\x{200D}]/u; next unless /$re/; my $k=join("", /$re/gu); print unless $seen{$k}++'
